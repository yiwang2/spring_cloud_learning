server:
  port: 8763
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
spring:
  application:
    name: user-service
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/springsecurityauth2
    username: root
    password: Pivotal20667
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
feign:
 hystrix:
   enabled: true